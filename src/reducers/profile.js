const initialState = {
  profile: {
    name: "名前",
    bio: "自己紹介",
    mail: "mail@gmail.com",
    pass: "password",
  }
};

const reducer = (state = initialState, action) => state;

export default reducer;
